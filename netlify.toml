[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.18.0"   # ← Supabase 2.84 exige Node 20+

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200