[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18.20.4"   # ← versão exata que nunca falha com Vite 5

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200